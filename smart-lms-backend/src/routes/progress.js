// routes/progress.js
const router = require('express').Router();
// router.get('/user/:user_id', async (req, res) => { ... }); // lấy tiến độ học tập
// router.post('/update', async (req, res) => { ... }); // cập nhật
module.exports = router;
